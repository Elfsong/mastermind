import React from "react";
import ReactDOM from "react-dom/client";
import QDSimulator from "./qd-ctf-simulator.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <QDSimulator />
  </React.StrictMode>
);
